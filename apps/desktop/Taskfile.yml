# https://taskfile.dev

version: "3"

silent: true

tasks:
  dev:
    desc: Run app
    dir: ./
    cmds:
      - cargo tauri dev

  build:
    desc: Build app
    dir: ./
    cmds:
      - cargo tauri build
