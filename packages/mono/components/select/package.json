{
  "name": "@sv-components/select",
  "version": "0.1.2",
  "description": "",
  "main": "dist/select.cjs.js",
  "module": "dist/select.es.js",
  "types": "src/index.ts",
  "scripts": {
    "dev": "histoire dev",
    "build": "vite build",
    "lint": "eslint --ext .ts .",
    "format": "prettier --ignore-path .gitignore --write \"**/*.+(ts|json)\"",
    "publish:patch": "npm run build && npm version patch && git push --tags && git push && npm publish"
  },
  "workspaces": [
    "."
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lit": "^2.5.0"
  },
  "devDependencies": {
    "vite": "^2.9.15"
  }
}
